import { ScrollSection } from './scroll-section';

describe('ScrollSection', () => {
  it('should create an instance', () => {
    const directive = new ScrollSection();
    expect(directive).toBeTruthy();
  });
});
